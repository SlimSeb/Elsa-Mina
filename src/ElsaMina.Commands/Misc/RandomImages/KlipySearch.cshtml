@inherits ElsaMina.Core.Services.Templates.LocalizableTemplatePage<ElsaMina.Commands.Misc.RandomImages.KlipySearchViewModel>

<table>
    <tbody>
    @for (var i = 0; i < Model.Gifs.Count; i += 4)
    {
        <tr>
            @for (var j = i; j < i + 4 && j < Model.Gifs.Count; j++)
            {
                var gif = Model.Gifs[j];
                <td style="padding:4px;text-align:center;">
                    <img src="@gif.PreviewUrl" width="@gif.ThumbWidth" height="@gif.ThumbHeight"
                         style="transform:rotate(0deg);display:block;" alt="Gif thumbnail"/>
                    <button class="button" name="send"
                            value="@Model.Trigger@("klipygif") @gif.FullUrl|@gif.FullWidth|@gif.FullHeight">@GetString("klipysearch_send")
                    </button>
                </td>
            }
        </tr>
    }
    </tbody>
</table>
