namespace ElsaMina.Commands.Misc.RandomImages;

/// <summary>
/// Quality tiers KLIPY exposes for every item. The enum names match the wire keys once lowercased.
/// </summary>
public enum KlipyMediaSize
{
    Hd,
    Md,
    Sm,
    Xs
}
