namespace ElsaMina.Commands.Misc.RandomImages;

/// <summary>
/// File formats KLIPY exposes for every size. The enum names match the wire keys once lowercased.
/// </summary>
public enum KlipyMediaFormat
{
    Gif,
    Webp,
    Jpg,
    Mp4,
    Webm
}
