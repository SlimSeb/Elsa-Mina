@* Templates live under their own namespace root so component names (Badge, Profile, Games...) never
   clash with command code types. The template key is the namespace path below it, see TemplatesManager. *@
@namespace ElsaMina.Templates
@preservewhitespace true
