@using ElsaMina.Core.Services.Templates
@using ElsaMina.Core.Utils
@inherits ElsaMina.Core.Services.Templates.LocalizableTemplatePage<ElsaMina.Commands.Games.Wordle.WordleLeaderboardViewModel>
@{
    const string headerStyle = $"border-bottom: 1px solid {StyleConstants.PRIMARY_BORDER_COLOR}; padding: 4px";
}
<div style="max-height: 200px; overflow-y: auto">
    <table style="margin: auto; border: 1px solid @StyleConstants.PRIMARY_BORDER_COLOR; border-radius: 12px; padding: 4px">
        <thead>
        <tr>
            <th colspan="5" scope="colgroup">@GetString("wordle_leaderboard_title")</th>
        </tr>
        <tr>
            <th scope="col" style="@headerStyle">#</th>
            <th scope="col" style="@headerStyle">@GetString("leaderboard_user")</th>
            <th scope="col" style="@headerStyle">@GetString("wordle_leaderboard_max_streak")</th>
            <th scope="col" style="@headerStyle">@GetString("wordle_leaderboard_wins")</th>
            <th scope="col" style="@headerStyle">@GetString("wordle_leaderboard_played")</th>
        </tr>
        </thead>
        <tbody>
        @for (int i = 0; i < Model.Leaderboard.Count; i++)
        {
            var entry = Model.Leaderboard[i];
            <tr>
                <td style="text-align:center">@(i + 1)</td>
                <td style="color: @entry.UserId.ToColorHexCodeWithCustoms()">@(entry.User?.UserName ?? entry.UserId)</td>
                <td style="text-align:center">@entry.MaxStreak</td>
                <td style="text-align:center">@entry.Wins</td>
                <td style="text-align:center">@entry.GamesPlayed</td>
            </tr>
        }
        </tbody>
    </table>
</div>
