@using ElsaMina.Commands.Badges.BadgeDisplay
@inherits ElsaMina.Core.Services.Templates.LocalizableTemplatePage<ElsaMina.Commands.Badges.BadgeHolders.BadgeHoldersViewModel>

<div style="max-height: 180px; overflow-y: auto;">

    @foreach (var badge in Model.Badges)
    {
        var badgeDisplayModel = new BadgeDisplayViewModel
        {
            Culture = Model.Culture,
            DisplayedBadge = badge,
            BadgeHolders = badge.BadgeHolders
                .Select(holding => holding.RoomUser?.User?.UserName ?? holding.UserId)
                .ToArray()
        };
        <text><ElsaMina.Templates.Badges.BadgeDisplay.BadgeDisplay Model="@(badgeDisplayModel)" /></text>
        <br/>
        <br/>
    }

</div>
